spring.main.banner-mode=off

# The judge below is an in-memory DummyChatModel so this example runs without network access.
# For a real judge, add the spring-ai-ollama (or spring-ai-openai) starter to pom.xml and
# configure it here, e.g.:
#   spring.ai.ollama.base-url=http://localhost:11434
#   spring.ai.ollama.chat.model=llama3.2
# then remove/replace the DummyChatModel bean.